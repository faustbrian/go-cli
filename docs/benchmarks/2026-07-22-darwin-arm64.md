# Benchmark evidence: 2026-07-22 Darwin arm64

> **Historical evidence only.** This capture predates the current Go 1.26.6
> minimum and used `urfave/cli` v3.5.0 rather than the current v3.10.1. Its raw
> samples are retained, but its cross-library values are not current
> compatibility or release evidence.

- source commit before evidence files: `63dd0b18c746378dbcfdb7304ec7b5e1689f73f8`
- Go: `go1.26.5 darwin/arm64`
- machine: Apple M4 Max, Darwin 27.0.0 arm64
- root fixture SHA-256: `b8604f6e7fcbb0f42576ca01b960f32a8bc6d8d62f3da082922650afcd6eafc7`
- comparison fixture SHA-256: `f58cd62f1e9a15d9334e761757229918565c0f058d5ddb12cc1cd5ae2a21e12c`
- warm-up and evidence duration: `-benchtime=50ms`
- root samples: 5; comparative samples: 10
- raw evidence: [`raw/2026-07-22-darwin-arm64.txt`](raw/2026-07-22-darwin-arm64.txt)

## Historical matched fixture

The cases shared the `deploy --force target` scenario, but did not perform
equivalent work. Framework-specific validation, output shapes, lifecycle work,
mutable-state reuse, and first-dispatch setup remained inside different timed
paths. Construction and dispatch results below preserve the benchmark labels
and raw samples used at the time; they do not establish normalized
cross-library ratios. The current
[harness boundary](../../benchmarks/README.md#comparison-boundary) documents
the successor fixture's per-case work.

| Framework | Construction median | Prepared dispatch median | Dispatch allocations |
| --- | ---: | ---: | ---: |
| `cli` | 1.489 µs | 3.251 µs | 82 |
| Cobra v1.10.2 | 360.9 ns | 1.222 µs | 24 |
| `urfave/cli` v3.5.0 | 211.8 ns | 7.384 µs | 178 |
| Kong v1.16.0 | 6.120 µs | 21.98 µs | 136 |
| standard `flag` | 109.1 ns | 107.9 ns | 1 |

The dispatch samples for `urfave/cli`, Kong, and standard `flag` were noisy on
this short audit run (95% median intervals of 77%, 48%, and 70%). The raw data
and `benchstat` report retain that uncertainty; no cross-framework speed claim
is drawn from it. The sample met the then-documented targets of four times
Cobra latency and no more than 100 allocations, but those targets were not
enforced by the repository benchmark gate.

Standard `flag` is a parsing floor and manually dispatches the subcommand
because it has no command tree. It is not feature-equivalent. The other cases
also performed different setup, validation, lifecycle, and output work, so the
recorded differences cannot be attributed to one framework guarantee.

## Owned scenarios

The root data separately measures small, broad, deep, and maximum graph
construction; root and deep dispatch; typed conversion; usage and validation
failures; suggestions; help; completion; manifests; JSON output; and
cancellation. For orientation, medians included 8.283 µs for a small compile,
1.222 ms for a broad compile, 3.365 ms for a maximum-command compile,
27.17 µs for root dispatch, and 432.2 ns for already-cancelled execution.

## Binary-size measurement

The trimmed reference process fixture was built with:

```sh
GOWORK=off go build -trimpath -o /tmp/cli-process-fixture \
  ./cmd/process-fixture
```

The binary was 4,658,706 bytes. `go tool nm -size` attributed 79,056 bytes to
symbols containing the `cli` module import path. This attribution omits
shared runtime, standard-library, Cobra, and linker effects, so it is an
auditable lower bound rather than a standalone library contribution claim.

These results describe this fixture, toolchain, and machine only. They are
retained as regression evidence, not release marketing claims.
